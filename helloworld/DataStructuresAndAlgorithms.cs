using System;
class DataStructuresAndAlgorithm()
{
    static void Arrays()
    {
        int[] numbers = { 1, 2, 3, 4, 5, 6 };

        for (int i = 0; i < numbers.Length; i++)
        {
            Console.WriteLine(i + "." + numbers[i]);
        }
            
    }
}